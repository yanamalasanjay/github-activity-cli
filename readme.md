# GitHub Activity CLI

A simple command-line tool to fetch and display the recent GitHub activity of any user.  
Built using pure Node.js **without any external libraries**.

---

## 🚀 Features

- Fetch recent GitHub events for any username
- Clean, human-readable CLI output
- Works globally as a command (`github-activity`)
- No external dependencies
- Lightweight and fast

---

## 📦 Installation (Local Development)

Clone the repo:

```bash
git clone https://github.com/<yanamalasanjay>/github-activity-cli.git
cd github-activity-cli
project url: https://roadmap.sh/projects/github-user-activity
